opencv-python==4.10.0.84
diffusers>=0.31.0
transformers==4.49.0
tokenizers==0.21.1
accelerate==1.6.0
tqdm
imageio
easydict
ftfy
dashscope
imageio-ffmpeg
numpy>=1.23.5,<2
xfuser

# Optional dependencies for FP8 quantization
# Uncomment the following lines for FP8 support:
# torch>=2.3.0  # For native FP8 support
# transformer-engine  # For optimized FP8 on NVIDIA GPUs
# psutil  # For memory monitoring
# gputil  # For GPU monitoring
